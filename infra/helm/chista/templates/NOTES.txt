Chista {{ .Chart.AppVersion }} (skeleton chart) deployed.

This chart currently provisions the API and web tiers only. The agent
(DaemonSet), the connection proxy, per-session Pod + Ingress routing, and HPA
autoscaling land in Phase 4 — see docs/architecture.

  kubectl get pods -l app.kubernetes.io/part-of=chista

Point an Ingress / LoadBalancer at the chista-web and chista-api Services for
host {{ .Values.domain }}.
